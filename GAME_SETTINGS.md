# ⚙️ 遊戲數值設定表 (Game Settings)

本文件整理「單字冒險 Word Quest」所有可調整的遊戲參數，以及對應的程式位置。
調整平衡時，依表格找到檔案中的常數或數值修改即可，改完重新整理瀏覽器就生效。

---

## 一、戰鬥系統 (js/systems/battle.js)

### 交互回合制
| 參數 | 目前數值 | 程式位置 | 說明 |
| --- | --- | --- | --- |
| 玩家傷害公式 | `總攻擊 × 技能倍率 × 屬性 × 本系 × 暴擊 − 敵防禦` | `useSkill()` | 最低保底 1 點 |
| 敵方傷害公式 | `敵攻擊力 − 我方總防禦 × 防禦Buff` | `enemyTurn()` | 最低保底 1 點 |
| 傷害隨機浮動 | ±10%（0.9 ~ 1.1 倍） | `useSkill()` / `enemyTurn()` 的 `0.9 + Math.random() * 0.2` | |
| 答錯擦傷 | 正常傷害的 **10%** | `useSkill()` 的 `full * 0.1` | 攻擊落空時的微量傷害 |

### 暴擊 (Critical Hit)
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 暴擊倍率 | **1.5 倍** | `useSkill()` 的 `critMult` |
| 觸發條件①（快答） | 用時 < 限時的 **35%** | `result.timeRatio < 0.35` |
| 觸發條件②（完美拼字） | 填空/拼字題無退回 且 用時 < 75% | `result.noMistake && result.timeRatio < 0.75` |

### QTE 完美格擋
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 限時 | **2500 毫秒（2.5 秒）** | 常數 `QTE_TIME_MS`（倒數條動畫自動同步） |
| 觸發機率 | **40%** | `enemyTurn()` 的 `chance(0.4)` |
| 格擋效果 | 傷害 **減半（×0.5）** | `blockMult = 0.5` |
| 出題範圍 | 只出 Lv1 簡單單字、缺 1 個字母、3 個選項 | `qteBlock()` |

### SP 能量系統
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| SP 上限 | **5 點** | `SP_MAX`（js/data/skills.js） |
| 普攻答對獲得 | +1 SP | skills.js `tackle.gainSp` |
| 施放失敗 | 仍扣除 SP（風險與報酬） | `useSkill()` 答錯分支 |

### 五行屬性
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 相剋攻擊加成 | **1.5 倍**（效果拔群） | js/data/skills.js `elemMultiplier()` |
| 被剋攻擊削弱 | **0.5 倍**（效果不佳） | 同上 |
| 本系加成 (STAB) | **1.2 倍** | battle.js `stab` |
| 相生防具加成 | 防具防禦 ×1.2 再 +20% 基礎防禦 | js/systems/monster.js `monsterStats()` |
| 相剋關係 | 金剋木、木剋土、土剋水、水剋火、火剋金 | skills.js `ELEM_KE` |
| 相生關係 | 金生水、水生木、木生火、火生土、土生金 | skills.js `ELEM_SHENG` |

### 戰敗與復活
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 錯題複習題數 | **3 題**（優先出答錯過的字） | `battleDefeat()` 迴圈 `i < 3` |
| 複習後恢復 | 全隊 **100%** HP，回到最近營火 | `battleDefeat()` |

---

## 二、題目與學習系統 (js/core/learning.js)

### 題型難度（依地圖）
| 地圖 | 一般戰鬥 | BOSS 戰 | 程式位置 |
| --- | --- | --- | --- |
| 翠綠草原 | 選擇題 | 填空 | `pickDifficulty()` |
| 碧水湖畔 | 55% 填空 / 45% 選擇 | 拼字 | 同上 |
| 烈焰火山 | 60% 填空 / 40% 拼字 | 拼字 | 同上 |
| 金石礦山 | 50% 填空 / 50% 拼字 | 拼字 | 同上 |
| 終極大招 | 一律強制「拼字」 | — | skills.js 各大招的 `hardQ: true` |

### 答題倒數計時
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 翠綠草原（新手區） | **不倒數** | `questionTimer()` |
| 選擇題 | 15 秒 | 同上 |
| 填空題 | 25 秒 | 同上 |
| 拼字題 | 35 秒 | 同上 |

### 題目細節
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 選擇題選項數 | 4 個 | `askQuestion()` easy 分支 |
| 填空隱藏比例 | 單字長度的 45%（最少 1、最多 4 個字母） | `clamp(Math.ceil(len * 0.45), 1, 4)` |
| 填空干擾字母 | 3 個 | `nDistract` |
| 拼字干擾字母 | 2 ~ 4 個 | 同上 |
| 智慧眼鏡效果 | 選擇題自動刪去 1 個錯誤選項 | easy 分支的 `passives.glasses` |

### 間隔重複（加權出題）
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 基礎權重 | 1 | `pickQuestionWord()` |
| 每答錯一次 | 權重 **+2.5** | `s.ng * 2.5` |
| 上次剛答錯（連對中斷） | 額外 +1.5 | `s.streak === 0` |
| 每連續答對一次 | 權重 −0.55（最多算 3 次） | `Math.min(s.streak, 3) * 0.55` |
| 權重下限 | 0.15 | `Math.max(0.15, wt)` |
| 「精通」門檻 | 連續答對 **3 次** | state.js `wrongWordPool()`、screens.js 單字書 |

### 單字庫 (js/data/words.js)
| 參數 | 目前數值 |
| --- | --- |
| 總字數 | 約 395 字 |
| 難度分級 | Lv1（短/高頻）→ Lv2（中等）→ Lv3（長字），對應地圖 `wordLv` |

---

## 三、收服系統 (js/systems/battle.js `tryCapture()`)

| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 基礎收服率 | 依怪獸 **15% ~ 30%** | js/data/monsters.js 各種族 `catchRate` |
| 完美答題加成 | **×1.5**（全場無答錯） | `Battle.perfect` |
| 收服球加成 | **×2**（一次性消耗） | `CONSUMABLES.ball` |
| 收服率上限 | **90%** | `Math.min(0.9, rate)` |
| 收服等級 | 敵方等級 −1（最低 1） | `createMonster(e.sp, e.lv - 1)` |
| 不可收服 | BOSS、精英怪（⭐）、初始夥伴系列 | `catchRate: 0` / `!e.elite` |
| 隊伍上限 | **3 隻**（超過自動進倉庫） | `G.team.length < 3` |

---

## 四、養成系統 (js/systems/monster.js)

### 經驗與升級
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 升級所需經驗 | `25 × 等級^1.4` | `expToNext()` |
| 戰鬥獲得經驗 | `種族經驗值 × (0.7 + 敵方等級 × 0.18)` | battle.js `battleVictory()` |
| 待命隊員經驗 | 出戰者的 **50%** | 同上 |
| 升級時 | 全滿血（兒童友善） | `gainExp()` |

### 進化
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 第一次進化 | **Lv.10** | monsters.js 各初始夥伴 `evolveLv` |
| 第二次進化 | **Lv.25** | 同上 |
| 終極大招習得 | **Lv.15**（第二型態的技能表） | monsters.js `iron2/wood2/water2/fire2/earth2` 的 `learnset` |
| 技能欄上限 | **4 格** | screens.js `openSkills()`、monster.js `equipSkill()` |

### 精英怪（錯題怪）
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| HP 加成 | ×1.3 | monster.js `enemyStats()` |
| 攻擊加成 | ×1.25 | 同上 |
| 防禦加成 | ×1.15 | 同上 |
| 經驗/金幣加成 | ×1.5 | battle.js `eliteMult` |
| 初次生成機率 | **35%**（有錯題時） | map.js `MapView.load()` 的 `chance(0.35)` |
| 重生時機率 | **60%**（錯題優先再現） | map.js `checkRespawn()` 的 `chance(0.6)` |
| 出題 | 只出玩家答錯過的單字 | learning.js `elite` 參數 |

---

## 五、獎勵與經濟 (js/systems/battle.js `battleVictory()`)

| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 金幣掉落 | `10 + 敵方等級 × 4` | `battleVictory()` |
| 幸運護符 | 金幣 **×1.5** | `passives.charm` |
| 裝備掉落機率 | **8%**（限當前商店等級內的裝備） | `chance(0.08)` |
| 初始金幣 | 100 | state.js `defaultState()` |
| 初始道具 | 藥水 ×2、收服球 ×1 | 同上 |

### 商店 (js/data/items.js) — 五級制
| 參數 | 目前數值 |
| --- | --- |
| 商店等級解鎖 | 開局 1 → 草原 BOSS→2 → 火山 BOSS→3 → **峽谷 BOSS→4 → 化石沙漠 BOSS→5**（`TIER_UNLOCK`／`MAX_TIER`） |
| 武器 | 木棒+2(50)、鐵劍+5(150)、字典之劍+9(400)、勇者聖劍+14(900)｜**T4** 秘銀劍+24(2400)｜**T5** 屠龍大劍+36(5500)、星辰神劍+50(12000) |
| 防具 | 布衣+2、字母盾牌+5/10、五行聖盾+8/20、龍鱗鎧甲+13/40｜**T4** 秘銀鎧甲+22/65(2600)、五行神盾+26/60(3200,相生加成)｜**T5** 守護聖鎧+34/100(6000)、星空神鎧+45/150(13000) |
| 消耗品 | 藥水 50%(30)、大藥水 100%(80)、收服球 ×2(60)｜**T4** 團隊聖水全隊全滿(350)、大師球 ×3(400) |
| 被動道具 | 智慧眼鏡(500)、幸運護符 金幣+50%(400)｜**T4** 經驗護符 EXP+50%(2500) — 買一次永久生效 |

---

## 六、地圖與探索 (js/ui/map.js、js/data/maps.js)

| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 返回門 | 每關（第 2 關起）起點旁的 🔙 可回上一關，重生於上一關出口旁（雙向通道） | maps.js 圖例 `R`、map.js `onReturn()` |
| 寶箱數 | 每關 **2 個**（各自獨立記錄開啟狀態，內容同該關獎勵） | maps.js 圖例 `X`、`G.world.chests` 以座標為 key |
| 裝飾地物 | 每關專屬可行走裝飾（草原🌼🌾、湖畔🌺⛱️、火山💎🦴、礦山⛏️🕸️） | maps.js `theme.deco`（小寫字母圖例） |
| 放生 | 倉庫怪獸可放生：回贈 **等級×15** 金幣、自動歸還身上裝備 | screens.js `data-release` |
| 扣血動畫 | 血條 1.1 秒緩動、傷害數字漂浮 1.7 秒、攻擊後停頓 1.35 秒 | style.css `.hpfill`/`.dmg-num`、battle.js `wait(1350)` |
| 怪獸重生時間 | **3 分鐘** | map.js 常數 `RESPAWN_MS` |
| 戰後無敵緩衝 | **3 秒**（怪獸主動碰撞不觸發） | map.js `afterBattle()` 的 `graceUntil` |
| 怪獸巡邏間隔 | 850 毫秒 / 每次移動機率 55% | map.js `startLoops()` |
| 巡邏範圍 | 出生點 ±2 格 | map.js wander 檢查 |
| 玩家移動間隔 | 140 毫秒/步 | map.js `MapView.move()` |
| 地圖尺寸 | 15 × 10 格、每格 44px | maps.js `TILE`、各 `grid` |
| 營火效果 | 全隊滿血＋設定重生點＋自動存檔 | map.js `onCampfire()` |

### 各地圖數值 (js/data/maps.js) — 五行 × 三循環＝15 關
第一循環出沒一階、第二循環二階、第三循環三階（含 ✨初始夥伴對應型態）；BOSS 由五王以更高等級輪迴鎮守。

| # | 地圖 | 屬性 | 敵方等級 | BOSS 等級 | 出沒階段 |
| --- | --- | --- | --- | --- | --- |
| 1 | 翠綠草原 | 木 | 1~3 | 橡樹王 5 | 一階 |
| 2 | 碧水湖畔 | 水 | 4~7 | 大王烏賊 8 | 一階 |
| 3 | 烈焰火山 | 火 | 8~12 | 炎龍王 13 | 一階 |
| 4 | 金石礦山 | 金 | 12~16 | 鋼鐵魔像 18 | 一階 |
| 5 | 黃土峽谷 | 土 | 16~20 | 遠古大地龍 22 | 一階 |
| 6 | 幽暗森林 | 木 | 20~24 | 橡樹王 26 | 二階 |
| 7 | 珊瑚海灣 | 水 | 24~28 | 大王烏賊 30 | 二階 |
| 8 | 熔岩洞窟 | 火 | 28~32 | 炎龍王 34 | 二階 |
| 9 | 齒輪工廠 | 金 | 32~36 | 鋼鐵魔像 38 | 二階 |
| 10 | 化石沙漠 | 土 | 36~40 | 遠古大地龍 42 | 二階 |
| 11 | 神木天域 | 木 | 40~45 | 橡樹王 48 | 三階 |
| 12 | 冰霜冰原 | 水 | 45~50 | 大王烏賊 53 | 三階 |
| 13 | 炎獄王城 | 火 | 50~55 | 炎龍王 58 | 三階 |
| 14 | 天空鋼都 | 金 | 55~60 | 鋼鐵魔像 63 | 三階 |
| 15 | 大地聖殿 | 土 | 60~66 | 遠古大地龍 70 | 三階 |

### 全怪獸三段進化
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 野生怪獸進化等級 | 一階 **Lv.20** → 二階 **Lv.40** → 三階 | monsters.js 底部進化鏈設定 |
| 初始夥伴進化等級 | Lv.10 / Lv.25（維持不變） | 各 starter `evolveLv` |
| 二階收服率 | 一階 −10%（最低 6%）；三階：一階 −15%（最低 5%） | 各 stage2/3 `catchRate` |
| 野外初始夥伴 | 二階收服率 10%、三階 8% | monsters.js 底部 |
| BOSS 進化等級 | 基本型 **Lv.25** → 二階 **Lv.45** → 三階（循環 1/2/3 分別鎮守基本/二階/三階型） | monsters.js BOSS 進化鏈 |
| BOSS 收服率 | 基本型 10%／二階 8%／三階 6% | 各 boss `catchRate` |
| 圖鑑總數 | **75 種**（20 線 ×3 階＋5 BOSS 線 ×3 階） | — |

### 重生與稀有收服
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| BOSS 重生 | 擊敗後 **5 小時**（出口解鎖永久有效） | maps.js `BOSS_RESPAWN_MS`、`G.world.bossDefeated` |
| BOSS 收服率 | **10%**（完美 ×1.5、收服球 ×2 照常適用） | monsters.js 各 boss `catchRate` |
| 初始夥伴出沒 | 各屬性地圖固定 ✨ 出沒點，**5 小時**重生 | maps.js `starterId`/`starterSpawn`、`STARTER_RESPAWN_MS` |
| 初始夥伴收服率 | **15%**（僅幼年期型態出沒） | monsters.js 五隻 stage1 `catchRate` |

### 敵方強度與怒氣
| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 敵方基礎傷害強化 | 攻擊力 **×1.25** | battle.js `enemyTurn()` |
| 怒氣加成 | 本場每答錯 1 次 **+15%**，**疊加無上限** | `B.wrongCount`、`rage` |

---

## 六之二、3D 版鏡頭 (3d/js/map3d.js、3d/js/battle3d.js)

| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 探索鏡頭距離 | 玩家身後 **4.5** 格 | map3d.js 常數 `CAM_DIST` |
| 探索鏡頭高度 | **3.2** | `CAM_HEIGHT` |
| 視線前望距離 | 前方 **2.2** 格 | `CAM_AHEAD` |
| 探索視野 (FOV) | 55° | `init3d()` 的 PerspectiveCamera |
| 鏡頭朝向 | **固定朝北、不旋轉**（防暈設計；改 `camAngle` 初始值可換方向） | map3d.js `camAngle: Math.PI` |
| 霧氣範圍 | 9 ~ 24 格（顏色隨地圖天空） | `build3dBoard()` 的 `THREE.Fog` |
| 操作模式 | 方向鍵直接對應地圖上下左右（與小地圖一致）；角色依移動方向左右翻面 | `MapView.move()`、`playerFlip` |
| 主角圖像 | 程式繪製「戴帽小冒險者」（探險帽/綠上衣/背包/靴子），2D/3D 共用 | js/core/avatar.js `Avatar.get()` |
| 小地圖更新頻率 | 每 150ms | `drawMinimap()` |
| 戰鬥鏡頭 | 位置 (0, 2.4, 6.2)、看向 (0, 1.05, −0.5)、FOV 46° | battle3d.js `init()` |

## 七、存檔系統 (js/core/state.js)

| 參數 | 目前數值 | 程式位置 |
| --- | --- | --- |
| 存檔位置數 | **3 格**（獨立插槽，可各自進行不同冒險） | 常數 `SLOT_KEYS`（state.js） |
| 存檔方式 | 瀏覽器 localStorage（`wordquest_save_s1/s2/s3`） | `saveGame()` / `loadSlot()` |
| 舊版存檔遷移 | 舊單一存檔自動搬移至位置 1 | `migrateOldSave()` |
| 選檔畫面摘要 | 隊長 emoji/名字/等級、地圖、金幣、存檔時間 | `slotInfo()`、main.js `slotLabel()` |
| 自動存檔時機 | 戰鬥結算後、購買後、裝備變更後、營火、切換地圖、關閉/切出頁面（寫入目前使用中的位置） | 各處 `autoSave()`、main.js `beforeunload` |
| 跨裝置存檔碼 | `WQZ:`（gzip+base64）優先，舊瀏覽器後備 `WQ1:`（純 base64）；匯入含格式驗證 | state.js `encodeSave()`/`decodeSave()`、main.js 存檔管理 |
| 舊存檔相容 | 載入時自動補齊新版欄位（`normalizeSave`）；基準存檔 `save_code.txt`，改版後開 `test_save.html` 驗證 12 項檢測全過 | state.js、test_save.html |

> **改版鐵則**：不可改名/刪除既有的怪獸、技能、裝備、地圖 id；存檔新增欄位要在 `normalizeSave()` 給預設值。這保證所有舊存檔（包含 save_code.txt 基準檔）永遠可以繼續玩。
| 存檔提示 | 🪶 羽毛筆圖示約 1.9 秒 | utils.js `showSaveToast()` |

---

## 八、怪獸種族數值 (js/data/monsters.js)

初始夥伴基礎值（Lv.1）與定位：
| 夥伴 | 屬性 | HP | ATK | DEF | 定位 |
| --- | --- | --- | --- | --- | --- |
| 小鐵甲 🐞 | 金 | 40 | 9 | 13 | 高防禦 |
| 芽芽獸 🌱 | 木 | 45 | 9 | 9 | 高回復（治癒技能） |
| 小水滴 💧 | 水 | 42 | 10 | 10 | 均衡 |
| 小火苗 🔥 | 火 | 38 | 13 | 8 | 高爆發 |
| 小岩怪 🪨 | 土 | 50 | 10 | 11 | 高生命 |

每隻怪獸的 `base`（基礎值）、`grow`（每級成長）、`learnset`（技能表）、`catchRate`（收服率）、`expYield`（擊敗經驗）都在 monsters.js 內逐一定義，可直接修改。

技能倍率與消耗在 js/data/skills.js（例：火焰衝擊 1.5 倍/3SP、終極大招 2.6 倍/5SP/強制拼字題）。
